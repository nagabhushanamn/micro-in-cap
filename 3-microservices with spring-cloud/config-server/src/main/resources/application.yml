

server:
  port: 8001



spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nagabhushanamn/micro-in-cap
          clone-on-start: true
          search-paths:
          - '3-microservices with spring-cloud/0-config-repo'
 
 
                    
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'